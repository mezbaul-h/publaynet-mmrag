"""PubLayNet multimodal RAG + knowledge graph proof-of-concept.

A staged retrieval-augmented generation pipeline that answers scientific
queries over the PubLayNet dataset, comparing a text-only baseline against an
enhanced approach that adds visual (figure) embeddings and a knowledge graph.
"""

__version__ = "0.1.0"
